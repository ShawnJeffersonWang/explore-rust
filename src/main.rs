use crate::as_ref::as_ref;
use crate::is_some::is_some_demo;
use crate::unwrap::unwrap_demo;

mod is_some;
mod as_ref;
mod unwrap;

fn main() {
    // is_some_demo();
    // as_ref();
    unwrap_demo();
}
